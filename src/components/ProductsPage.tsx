import { Box } from "@chakra-ui/react";
import ProductCard from "./ProductCard";

const ProductsPage = () => {
  return (
    <Box>
      <ProductCard />
    </Box>
  );
};

export default ProductsPage;
